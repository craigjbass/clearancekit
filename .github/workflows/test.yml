name: Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test
        run: |
          xcodebuild test \
            -scheme clearancekit \
            -destination 'platform=macOS'
